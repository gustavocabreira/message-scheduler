# Main app - localhost.com
server {
    listen 80;
    server_name localhost.com www.localhost.com;

    client_max_body_size 100M;

    location / {
        proxy_pass http://app:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }
}

# API subdomain - api.localhost.com
# Proxies to the app with /api prefix so that:
#   api.localhost.com/auth/huggy/callback -> app:8000/api/auth/huggy/callback
server {
    listen 80;
    server_name api.localhost.com;

    client_max_body_size 100M;

    location / {
        rewrite ^/(.*)$ /api/$1 break;
        proxy_pass http://app:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }
}
